/** Gio hang */

export interface CartItemType {
  variantId: string;
  productId: string;
  name: string;
  slug: string;
  color: string;
  color_hex: string;
  size: string;
  price: number;
  compare_at_price: number;
  image: string;
  qty: number;
  sku: string;
  max_qty: number;
}
